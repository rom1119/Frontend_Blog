{% extends 'base.html.twig' %}

{% block body %}
	
	<table>
		
			{% for item in form %}
				<tr>
					<td>{{ item.name }}</td>
					<td>{{ item.price }}</td> 
					<td>{{ item.description }}</td>
				</tr>
			{% endfor %}
		

	</table>

{% endblock %}
